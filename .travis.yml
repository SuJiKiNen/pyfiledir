dist: xenial   # required for Python >= 3.7
language: python
matrix:
    - os: linux
      language: python
      python:
        - '3.6'
        - "3.7"

install:
  - pip install pipenv
  - pipenv install --dev --system
script:
  - pytest

language: python
matrix:
  include:
    - os: linux
      language: python
      python: '3.5'
    - os: linux
      language: python
      python: '3.6'
    - os: linux
      language: python
      python: '3.7'
      dist: xenial   # required for Python >= 3.7

install:
  - pip install pipenv
  - pipenv install --dev --system
script:
  - pytest
